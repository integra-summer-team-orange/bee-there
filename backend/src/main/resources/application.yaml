server:
  port: 8080

jwt:
  secret: p39gn67vfql39457th593403nbvunxmzlaien39o
  expiration-ms: 86400000 #24h

spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://localhost:5432/bee-there
    username: admin
    password: pass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  flyway:
    enabled: true

logging:
  level:
    org.hibernate.SQL: debug

